"
This class has no behavior in it. It exist only to define methods named exactly as the handled pragmas, so they are suggested by the autocompletion engine
"
Class {
	#name : #QuickWrapPragmaCompletionHelper,
	#superclass : #Object,
	#category : #'QuickWrap-Core'
}
